<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="Divider_Style">
        <item name="android:listDivider">@drawable/diver_backgrounde</item>
    </style>

    <style name="dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>   <!-- 边框 这里表示无边框-->
        <item name="android:windowIsFloating">true</item>  <!-- 是否悬浮在activity之上-->
        <item name="android:windowIsTranslucent">false</item><!-- 半透明-->
        <item name="android:windowNoTitle">true</item>  <!-- 无标题-->
        <item name="android:windowBackground">@color/vifrification</item>   <!-- 背景透明  这个就是我需要的关键的效果-->
        <item name="android:backgroundDimEnabled">false</item><!-- 模糊-->
    </style>

    <style parent="@android:style/Theme.Dialog" name="NoDialogTitle">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <!--下面的这个颜色也是透明的 RGB的数值是  #00000000-->
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <!--//设置dialog的背景颜色-->
        <item name="android:background">#ffffff</item>
    </style>
    <!--这个用于设置dialog的弹出和弹入的动画  -->
    <style name="dialogWindowAnim" parent="android:Animation" mce_bogus="1">
        <item name="android:windowEnterAnimation">@anim/dialogwindowanim</item>
        <item name="android:windowExitAnimation">@anim/dialog_ex_animation</item>
    </style>


</resources>
